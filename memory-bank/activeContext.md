# Active Context

Current focus:

- keep the repository clean and minimal
- maintain direct imports to `core` and `domain`
- preserve server-first deployment workflow

Immediate follow-up:

- reinstall dependencies before the next build/test cycle
- decide whether frontend package lockfiles should be committed in the future

